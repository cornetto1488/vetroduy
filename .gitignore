# зависимости — ставятся через npm install
node_modules/

# результат сборки и установщики (они идут в GitHub Releases, не в репозиторий)
dist/

# логи и мусор
*.log
.DS_Store
Thumbs.db

# bin/yt-dlp.exe (18 МБ) оставляем в репо — тогда сборка работает на любом ПК.
# Если не хочешь бинарник в репе — раскомментируй строку ниже и качай его по README:
# bin/
